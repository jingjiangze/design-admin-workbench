# Wrangler 本地开发密钥模板（短时本地调试专用）
# 用法：cp .dev.vars.example .dev.vars（放项目根！）然后填入真实值
# ⚠️ [VERIFIED 2026-09-21] .dev.vars 必须放在 wrangler.jsonc 同目录（项目根）——
#     放 worker/ 子目录 wrangler 读不到（wrangler 按配置文件目录查找）。
# ⚠️ .dev.vars 已在 .gitignore（任意目录级规则）—— 绝对禁止提交（长文 §三十四）
# ⚠️ wrangler.jsonc 顶层 vars 会覆盖 .dev.vars 同名变量；本地开 legacy 用命令行：
#     npx wrangler dev --port 8787 --var LEGACY_API_ENABLED:true
# Turnstile 本地真实验证用官方测试 secret（任何 token 恒通过）：
#     TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA
#     （注意没有连字符；带连字符会报 invalid-input-secret）
# 生成 32 字节密钥（PowerShell）：
#   [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
# 生成 32 字节密钥（Node）：
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

SESSION_ENCRYPTION_KEY=replace-with-base64-32-bytes
# 生产登录防护需要；本地 Mock 调试可留空
TURNSTILE_SECRET_KEY=
