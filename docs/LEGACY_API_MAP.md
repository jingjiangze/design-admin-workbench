# LEGACY_API_MAP.md — 原系统接口地图

> 状态：**NOT VERIFIED（未验证）** — 待 Network 面板逐页取证后填写。
> 只记录请求结构，不执行任何有副作用的操作（催单等只取证不真实发送）。

## 接口清单

| API | Method | 功能 | 参数 | 返回 | 鉴权 | 风险 | 证据等级 |
| --- | ------ | ---- | ---- | ---- | ---- | ---- | -------- |
| （待取证） | | | | | | | |

## 关键取证项

- [ ] 登录/Session 机制（Cookie / Token / CSRF / 验证码）
- [ ] 订单列表（分页参数、排序参数、筛选参数）
- [ ] 订单查询 / 订单详情
- [ ] 订单状态与操作记录
- [ ] 催单接口（URL / Method / 参数 / Header / 返回 / 是否幂等 / 是否有批量接口）
- [ ] 设计任务 / 品类 / 客户 / 操作记录
- [ ] 搜索 / 批量操作

## 留样规范

关键 JSON 返回必须脱敏后存放于 `research/raw/`；账号、密码、Cookie、Token、Session ID 一律 `<REDACTED>`。
